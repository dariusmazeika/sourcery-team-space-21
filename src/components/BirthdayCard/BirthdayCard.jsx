import React from "react";
// import styles from "./birthday-card.module.scss";d
import PropTypes from "prop-types";

export const BirthdayCard = (data) => {
  return <div></div>;
};

BirthdayCard.propTypes = {
  data: PropTypes.object,
};
